# docs

Notes and references to keep theme decisions consistent.

Suggested topics to capture here:
- How palette tokens are named
- Contrast targets (normal text, subtle text, selection, etc.)
- How to map semantic roles to each tool
- Export mapping rules for generated download endpoints
